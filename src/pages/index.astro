---
import Layout from '../layouts/Layout.astro';
import Home from '../components/Home/Home';
import Footer from '../components/Footer/Footer.astro';
---

<Layout title='HASUROのblog'>
    <div class='home-container'>
        <div class='top-text-container'>
            <p class='top-title'>
                HASURO's <span>BLOG</span>
            </p>
            <p class='top-text'>
                HASUROのブログです。ここは、HASUROが気まぐれに記事を投稿するだけのサイトです。
            </p>
            <a href='https://hasuro.com'' class='top-link' target='_blank'>
                Read my profile<span>→</span>
            </a>
        </div>
        <Home client:load />
    </div>
    <Footer />
</Layout>

<style>
    .home-container {
        width: 100%;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        justify-content: flex-start;
        gap: 60px;
        padding: 0 20px;
    }

    .top-text-container {
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        justify-content: flex-start;
        gap: 20px;
        margin-top: 70px;
    }

    .top-title {
        font-size: 50px;
        font-weight: 700;
    }

    .top-title span {
        background: linear-gradient(135deg, rgba(88,186,137,1) 0%, rgba(216,223,0,1) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        color: var(--primary-green);
    }

    .top-text {
        font-size: 30px;
        font-weight: 600;
    }

    .top-link {
        font-size: 30px;
        font-weight: 600;
        background: linear-gradient(135deg, rgba(88,186,137,1) 0%, rgba(216,223,0,1) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .top-link span {
        padding-left: 10x;
    }

    .top-link:hover span {
        padding-left: 10px;
    }
</style>

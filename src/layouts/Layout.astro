---
import Header from '../components/Header/Header';
import ColorThemeProvider from '../middlewares/ColorThemeProvider';
interface Props {
	title: string;
	url: string;
}

const { title, url } = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, 'https://blog.hasuro.com');
const ogpImageURL = new URL('/opengraph-image.png', canonicalURL);
const description = 'HASUROのブログです。'
---

<!doctype html>
<html lang="ja">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="HASUROのブログ" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
		<title>{title}</title>
		<meta name="generator" content={Astro.generator} />
		<meta name="title" content={title} />
		<meta name="description" content={description} />
		<meta property="og:type" content="website" />
		<meta property="og:url" content={Astro.url} />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:image" content={ogpImageURL} />
		<meta property="twitter:card" content="summary_large_image" />
		<meta property="twitter:url" content={Astro.url} />
		<meta property="twitter:title" content={title} />
		<meta property="twitter:description" content={description} />
		<meta property="twitter:image" content={ogpImageURL} />
		<meta name="google-site-verification" content="vy6iO3bOMhqBRcRKIYJxEM3ANIwaO2EJjTpiQpi0Tus" />
	</head>
	<body>
		<ColorThemeProvider client:load>
			<Header client:load />
			<div class='all-wrappar'>
				<div class='all-container'>
					<slot />
				</div>
			</div>
		</ColorThemeProvider>
	</body>
</html>
<style is:global>
	:root {
		--primary-green: 88, 186, 137;
		--primary-yellow-green: 216, 223, 0;
		--primary-pink: 226, 56, 58;
		--primary-pale-pink: 238, 131, 133;
		--primary-gray: 145, 158, 153;

		--link-color: 0, 170, 235;

		--dark-blue: 32, 50, 63;
	}

	:root[color-theme='light'] {
		--background-color: 229, 236, 237;
		--background-theme-color: 255, 255, 255;

		--code-background-color: 240, 240, 240;

		--text-color: 0, 0, 0;
		--text-color-rgb: rgb(0, 0, 0);

		--justify-content: flex-start;

		--change-button-gradation: linear-gradient(135deg, rgba(233,70,85,1) 0%, rgba(243,163,130,1) 100%);

		--box-shadow: 0 0 1px rgb(var(--primary-pink)), 0 0 2px rgb(var(--primary-pink)), 0 0 3px rgb(var(--primary-pink)), 0 0 5px rgb(var(--primary-pale-pink));
	}

	:root[color-theme='dark'] {
		--background-color: 0, 0, 0;
		--background-theme-color: 26, 32, 35;

		--code-background-color: 40, 46, 49;

		--text-color: 255, 255, 255;
		--text-color-rgb: rgb(255, 255, 255);

		--justify-content: flex-end;

		--change-button-gradation: linear-gradient(135deg, rgba(88,186,137,1) 0%, rgba(216,223,0,1) 100%);

		--box-shadow: 0 0 1px rgb(var(--primary-green)), 0 0 2px rgb(var(--primary-green)), 0 0 3px rgb(var(--primary-green)), 0 0 5px rgb(var(--primary-yellow-green));
	}

	* {
		box-sizing: border-box;
		padding: 0;
		margin: 0;
	}

	html,
	body {
		font-family: system-ui, sans-serif;
		max-width: 100vw;
		min-height: 100vh;
		overflow-x: hidden;
		color: var(--text-color-rgb);
		background-color: rgb(var(--background-color));
	}

	a {
		color: rgb(var(--primary-green));
		font-weight: 600;
	}

	h1 {
		width: 100%;
		margin-top: 2em;
		margin-bottom: 1.1rem;
		padding-bottom: 0.2em;
		border-bottom: 1px solid rgb(var(--primary-gray));
		font-size: 1.7rem;
	}

	h2 {
		width: 100%;
		margin-top: 36px;
		margin-bottom: 18px;
		padding-bottom: 5px;
		border-bottom: 1px solid rgb(var(--primary-gray));
		text-align: left:
	}

	h3 {
		width: 100%;
		margin-top: 20px;
		margin-bottom: 12px;
		text-align: left:
	}

	.article-main-container p {
		line-height: 1.9;
	}

	ol {
		margin-top: 0.6rem;
		margin-bottom: 1.4rem;
		padding-left: 1.7rem;
	}

	ol > li {
		margin: 0.4rem 0;
		line-height: 1.7rem;
	}

	.article-main-container img {
		width: 100%;
		max-width: 800px;
		margin: 0.6rem 0;
		border-radius: 4px;
	}

	pre {
		width: 100%;
		margin: 20px 0;
		padding: 18px;
		border-radius: 12px;
		background-color: rgb(var(--dark-blue));
		box-shadow: 0 4px 6px -1px rgba(var(--primary-gray), 0.1);
	}

	ul {
		margin-top: 11px;
		margin-bottom: 22px;
		padding-left: 25px;
	}

	pre > code {
		color: #ffffff;
		background-color: inherit;
	}

	code {
		padding: 0.2rem 0.4rem;
		border-radius: 4px;
		background-color: rgb(var(--code-background-color));
		font-family:
			Menlo,
			Monaco,
			Lucida Console,
			Liberation Mono,
			DejaVu Sans Mono,
			Bitstream Vera Sans Mono,
			Courier New,
			monospace;
	}

	blockquote {
		font-size: 1em;
    margin: 1.4rem 0;
    padding: 2px 0 2px .7em;
    color: rgb(var(--primary-gray));
    border-left: 3px solid rgb(var(--primary-gray));
	}

	table {
		margin: 1.2rem 0;
    width: auto;
    border-collapse: collapse;
    font-size: 0.95em;
    line-height: 1.5;
    word-break: normal;
    display: block;
    overflow: auto;
	}

	th {
		padding: 0.5rem;
		font-weight: 700;
    border: 1px solid rgb(var(--code-background-color));
		background-color: rgb(var(--code-background-color));
	}

	td {
		padding: 0.5rem;
    border: 1px solid rgb(var(--code-background-color));
	}

	.all-wrappar {
		width: 100%;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: flex-start;
	}

	.all-container {
        width: 100%;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        justify-content: flex-start;
		padding-top: 60px;
        @media screen and (min-width: 1100px) {
            width: 1100px;
        }
    }

	.non-style-link {
		text-decoration: none;
		color: inherit;
	}

	.fill-link {
		width: 100%;
	}
</style>
